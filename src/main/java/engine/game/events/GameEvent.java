package engine.game.events;

/**
 * Marker interface for all game-layer events.
 *
 * <p>Game events are pure data objects describing that something has happened in the game domain.
 *
 * <p>They must not contain engine references.
 */
public interface GameEvent {}
